# The user-facing entry. Static bytes are answered here from disk; only the
# session-bearing surface is proxied to the gateway.
#
# This is the layering the deployment wants: the frontend is the front door and
# the gateway is a backend for `/api`. Putting the gateway in front instead made
# every asset — index.html and 44 client bundles on a cold load — cross a Node
# process for no reason: measured at ~26% more latency per static request when
# nothing else competed for the event loop.

# The gateway is addressed through a variable, and this is why.
#
# An `upstream` block naming `gateway:8080` resolves that name ONCE, when the
# configuration loads, and holds the address for the life of the process. The
# gateway is a container: recreate it — a rebuild, a rolled-back image, an
# `up -d` that replaces it — and Docker hands it a new address on this network
# while nginx keeps sending to the old one. Every proxied route then answers
# 502, and `/` and `/app` answer 500, because their `auth_request` subrequest
# is one of the ones that failed. Nothing here restarts on its own, so the
# whole deployment stays down until somebody restarts THIS container — which
# is what happened for eight hours after a gateway rebuild, while the gateway
# itself was healthy and answering on its own port the entire time.
#
# A variable makes nginx resolve per request instead, through Docker's embedded
# DNS, and `valid=10s` bounds how stale an answer may be. The cost is the
# keepalive pool that an upstream block carries: those connections are held per
# upstream group, and a variable does not name one. Every proxied request now
# pays a TCP handshake to a process on the same bridge network — tens of
# microseconds — and that is the right side of this trade. An optimisation
# whose failure mode is a total outage is not an optimisation.
resolver 127.0.0.11 valid=10s ipv6=off;
include /etc/nginx/dsh-combos.conf;

# Held in a variable rather than written into each `proxy_pass`, so this file
# is the one place the gateway's address is stated.
map $host $gateway_upstream {
  default gateway:8080;
}

# Which file `/app` serves, which is how a failed backend becomes a redirect.
#
# `$recover` is filled by `auth_request_set` from the gateway's answer, and a
# map is evaluated where it is used — in the content phase, after the auth
# subrequest has run and set it. Naming a file that does not exist is what
# makes `try_files` fall through to the redirect; the shell itself is the
# ordinary answer.
map $recover $shell_document {
  ''      /index.html;
  default /the-backend-is-down;
}

# `Connection: upgrade` only for requests that actually carry an Upgrade header.
#
# The empty case is an EMPTY VALUE, not `close`. nginx's own WebSocket example
# says `close`, and pairing that with the keepalive above cancels it: every
# ordinary proxied request would ask the gateway to hang up the connection this
# is trying to reuse. Empty means nginx sends no Connection header at all, which
# with HTTP/1.1 is the persistent default.
map $http_upgrade $connection_upgrade {
  default upgrade;
  ''      '';
}

# Compression, which nothing was doing.
#
# The front door is 71 KB of markup that gzips to 22, and upstream's build ships
# a megabyte of JavaScript across two files. Both were served whole to every
# visitor.
#
# What is NOT listed matters as much as what is: images, fonts and archives are
# already compressed, and running them through gzip spends CPU to make them very
# slightly larger.
gzip on;
gzip_vary on;
gzip_comp_level 5;
# Below about a KB the header costs more than the saving, and a small response
# usually fits in the first round trip either way.
gzip_min_length 1024;
gzip_proxied any;
gzip_types
  text/plain
  text/css
  text/javascript
  application/javascript
  application/json
  application/manifest+json
  application/wasm
  image/svg+xml;

# What a single address may ask for, and how fast.
#
# The operator's console is on the public internet by decision, and one
# password plus one six-digit code is what stands in front of every account.
# The application counts failures too, but it counts them after having done the
# work — a hash, a database read — and an attacker's whole method is to make a
# service do that work a great many times. This refuses before any of it.
#
# Sign-in is the only thing rate-limited, because it is the only thing worth
# guessing at.
#
# Counted per POST, not per request. Only a POST is a guess; a GET renders the
# form, and somebody who reloads the page should not be spending a budget meant
# for attempts. An empty key is nginx's way of saying "do not count this one",
# which is what the map below produces for every other method.
map $request_method $admin_signin_key {
  POST    $binary_remote_addr;
  default "";
}

# Thirty a minute, and read that as one every two seconds rather than as a
# generous total: `6r/m` was here first, and `r/m` means the interval, not the
# allowance. One every ten seconds — applied, with the `burst` below, to every
# step of signing in — is what an operator experienced as a console that took
# half a minute to let them in.
#
# The real limit on guessing is in the application: eight failed attempts per
# address per ten minutes, and five wrong codes per challenge. This is here to
# blunt a flood before any work is done for it, not to be that limit.
limit_req_zone $admin_signin_key zone=admin_signin:1m rate=30r/m;

# The version number is not the visitor's business, and naming it only helps
# someone matching a deployment against a vulnerability list.
server_tokens off;

# Plain HTTP. It serves the same site as the TLS port, except where the
# deployment has been told its public HTTPS port — then this redirects there
# instead. `entrypoint.sh` writes that decision into the include below, and
# writes the file empty when there is nothing to redirect to, which is why this
# is a generated include rather than a `return` written here: nginx cannot work
# the address out for itself, since it sees the port it listens on and not the
# one the container publishes. Same reason `absolute_redirect` is off.
#
# The include sits before the site, but position is not what makes it win: a
# server-level `return` runs in the rewrite phase, before nginx has even chosen
# a location. When the file is empty, nothing happens and the site is served.
server {
  listen 80 default_server;
  server_name _;
  include /etc/nginx/redirect.inc;
  include /etc/nginx/site.inc;
}

# TLS exists because the application requires a secure context, not because
# anything here is confidential in transit. `crypto.randomUUID` — which the
# connection, runtime and conversation client plugins all call — is undefined
# on a page served over plain HTTP to anything but localhost, so the frontend
# fails on load when reached at a LAN address without this.
#
# The certificate is generated per deployment on first start; see
# `entrypoint.sh`. `compose.tls.yml` replaces it with one a browser trusts.
#
# `server_name _` on purpose, and not the domain: this deployment answers on
# whatever address reaches it — a domain, a LAN address, localhost — and one
# site serves all of them. Naming a host here would only matter if a second
# site had to be told apart from this one, and there is no second site.
# The operator's console, on a name of its own.
#
# Generated by `entrypoint.sh` from `ADMIN_DOMAIN`, and empty when that is
# unset — a deployment that has not decided to publish it does not publish it,
# and the decision is one variable rather than a file somebody edits.
include /etc/nginx/admin.inc;

server {
  # `default_server`, and it matters: the first server block on a listener is
  # the one nginx hands an unmatched name to, and the operator console's vhost
  # is included above this one. Without this, a request for any name nobody
  # configured — an IP address, a stale DNS record, a scanner walking the
  # certificate transparency log — arrived at the console instead of here.
  # Said explicitly so the answer stops depending on the order of two files.
  listen 443 ssl default_server;
  http2 on;
  server_name _;
  ssl_certificate /etc/nginx/tls/server.crt;
  ssl_certificate_key /etc/nginx/tls/server.key;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_session_cache shared:SSL:10m;
  include /etc/nginx/site.inc;
}
