{
  "name": "hamsterhq-verify",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Where the browser suites install Playwright, inside the container that runs them.",
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
