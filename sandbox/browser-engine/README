# Placeholder so COPY sandbox/browser-engine always has a source.
# A production host that builds with BROWSER_SOURCE=antidetect fills this
# directory from its latest Chromium chrome-dist/ (rsync) — see
# docs/cubesandbox.md. The binary is never committed.
